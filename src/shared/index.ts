/**
 * shared — recursos genuinamente transversais que NÃO pertencem ao Core
 * (ex.: assets estáticos, mocks, fixtures compartilhados).
 * Se algo for consumido por múltiplos módulos como funcionalidade, mova para Core.
 */
export {};
