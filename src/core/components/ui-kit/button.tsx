/**
 * Button — re-export do primitivo shadcn com tipos.
 * Mantido no Core para que módulos consumam sempre pelo Design System.
 */
export { Button, type ButtonProps } from "@/components/ui/button";
