import { createFileRoute } from "@tanstack/react-router";
import { ArrowRight, Boxes, Plug, Plus, RefreshCw, Search, ShoppingBag } from "lucide-react";
import { AppShell, Card } from "../components/AppShell";

const configs = { produtos: ["Produtos", "Seu catálogo centralizado, com SKUs, variações e preços.", "Novo produto", "Nenhum produto cadastrado", "Cadastre seu primeiro produto para começar a vender em todos os canais."], estoque: ["Estoque", "Controle saldos, reservas e movimentações em tempo real.", "Nova movimentação", "Seu estoque está vazio", "O estoque será atualizado automaticamente quando você cadastrar produtos e pedidos."], pedidos: ["Pedidos", "Todos os pedidos em uma operação única e organizada.", "Importar pedidos", "Nenhum pedido recebido", "Conecte um canal de venda para importar seus pedidos automaticamente."], clientes: ["Clientes", "Conheça seus clientes e construa relacionamentos duradouros.", "Novo cliente", "Nenhum cliente cadastrado", "Seus clientes aparecerão aqui quando você começar a vender."], canais: ["Canais de venda", "Conecte marketplaces, lojas e hubs sem duplicar seu trabalho.", "Adicionar canal", "Nenhum canal conectado", "Escolha um canal para sincronizar catálogo, estoque, pedidos e anúncios."] } as const;
export const Route = createFileRoute("/$module")({ component: ModulePage });
function ModulePage() { const { module } = Route.useParams(); const c = configs[module as keyof typeof configs] ?? configs.produtos; return <AppShell><div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-primary">Operação</p><h1 className="mt-1 text-3xl font-bold tracking-tight">{c[0]}</h1><p className="mt-2 text-muted-foreground">{c[1]}</p></div><button className="inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground"><Plus className="h-4 w-4" /> {c[2]}</button></div>{module === "canais" && <div className="mt-8 grid gap-4 md:grid-cols-3">{["Mercado Livre", "Shopee", "Shopify"].map(name => <Card key={name}><div className="flex items-center gap-3"><div className="grid h-10 w-10 place-items-center rounded-xl bg-primary/15 font-bold text-primary">{name[0]}</div><div><p className="font-semibold">{name}</p><p className="text-xs text-muted-foreground">Marketplace / loja virtual</p></div></div><button className="mt-5 flex w-full items-center justify-center gap-2 rounded-lg border border-border px-3 py-2 text-sm font-medium hover:bg-secondary"><Plug className="h-4 w-4" /> Conectar</button></Card>)}</div>}<Card className="mt-8"><div className="flex items-center justify-between border-b border-border pb-4"><div className="flex items-center gap-3"><Search className="h-4 w-4 text-muted-foreground" /><span className="text-sm text-muted-foreground">Buscar em {c[0].toLowerCase()}...</span></div><RefreshCw className="h-4 w-4 text-muted-foreground" /></div><div className="grid min-h-64 place-items-center text-center"><div><div className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-primary/10 text-primary"><Boxes className="h-6 w-6" /></div><p className="mt-4 font-semibold">{c[3]}</p><p className="mx-auto mt-2 max-w-sm text-sm text-muted-foreground">{c[4]}</p><button className="mt-5 inline-flex items-center gap-2 rounded-lg bg-secondary px-4 py-2 text-sm font-semibold">Começar agora <ArrowRight className="h-4 w-4" /></button></div></div></Card></div></AppShell>}
