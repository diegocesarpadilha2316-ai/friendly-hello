import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowUpRight, Boxes, CheckCircle2, ChevronRight, CircleDollarSign, PackageCheck, ShoppingCart, TrendingUp } from "lucide-react";
import { AppShell, Card } from "../components/AppShell";

export const Route = createFileRoute("/")({ component: Dashboard });
const metrics = [{ label: "Vendas hoje", value: "R$ 0,00", change: "Comece conectando um canal", icon: CircleDollarSign, color: "text-emerald-500" }, { label: "Pedidos", value: "0", change: "Aguardando sua primeira venda", icon: ShoppingCart, color: "text-blue-500" }, { label: "Produtos ativos", value: "0", change: "Cadastre seu catálogo", icon: Boxes, color: "text-violet-500" }, { label: "Estoque crítico", value: "0", change: "Tudo sob controle", icon: PackageCheck, color: "text-amber-500" }];
function Dashboard() { return <AppShell><div className="mx-auto max-w-7xl"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-primary">Dioris ERP</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Bom dia, Diego.</h1><p className="mt-2 text-muted-foreground">Sua operação multicanal começa aqui.</p></div><Link to="/produtos" className="inline-flex items-center justify-center gap-2 rounded-xl bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground hover:opacity-90">Adicionar produto <ArrowUpRight className="h-4 w-4" /></Link></div><div className="mt-8 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{metrics.map(({ label, value, change, icon: Icon, color }) => <Card key={label}><div className="flex items-center justify-between"><p className="text-sm text-muted-foreground">{label}</p><Icon className={`h-5 w-5 ${color}`} /></div><p className="mt-5 text-2xl font-bold">{value}</p><p className="mt-1 text-xs text-muted-foreground">{change}</p></Card>)}</div><div className="mt-6 grid gap-6 lg:grid-cols-[1.5fr_1fr]"><Card><div className="flex items-center justify-between"><div><h2 className="font-semibold">Vendas por canal</h2><p className="mt-1 text-sm text-muted-foreground">Conecte seus canais para acompanhar tudo em um só lugar.</p></div><TrendingUp className="h-5 w-5 text-primary" /></div><div className="mt-8 grid h-40 place-items-center rounded-xl border border-dashed border-border bg-secondary/30 text-center"><div><p className="text-sm font-medium">Nenhum dado ainda</p><Link to="/canais" className="mt-2 inline-flex items-center gap-1 text-xs font-semibold text-primary">Conectar canal <ChevronRight className="h-3 w-3" /></Link></div></div></Card><Card><h2 className="font-semibold">Primeiros passos</h2><div className="mt-4 space-y-4">{[{ label: "Configure sua empresa", to: "/configuracoes" }, { label: "Cadastre seu catálogo", to: "/produtos" }, { label: "Conecte um canal de venda", to: "/canais" }, { label: "Escolha seu plano", to: "/assinatura" }].map((item, i) => <Link key={item.to} to={item.to} className="flex items-center gap-3 rounded-lg p-2 hover:bg-secondary"><span className="grid h-6 w-6 place-items-center rounded-full bg-primary/15 text-xs font-bold text-primary">{i + 1}</span><span className="flex-1 text-sm">{item.label}</span><CheckCircle2 className="h-4 w-4 text-muted-foreground/40" /></Link>)}</div></Card></div></div></AppShell>; }
