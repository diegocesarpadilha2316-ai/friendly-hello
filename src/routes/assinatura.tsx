import { createFileRoute } from "@tanstack/react-router";
import { Check } from "lucide-react";
import { AppShell, Card } from "../components/AppShell";
export const Route = createFileRoute("/assinatura")({ component: SubscriptionPage });
function SubscriptionPage() { const plans = [{name:"Essencial", price:"R$ 49", desc:"Para começar a vender", features:["500 produtos","1 canal de venda","1 usuário"]},{name:"Profissional",price:"R$ 129",desc:"Para operações em crescimento",features:["Produtos ilimitados","5 canais de venda","5 usuários","Automação de estoque"]},{name:"Scale",price:"R$ 299",desc:"Para operações multicanal",features:["Canais ilimitados","Usuários ilimitados","API e webhooks","Suporte prioritário"]}]; return <AppShell><div className="mx-auto max-w-6xl"><p className="text-sm font-medium text-primary">Dioris ERP</p><h1 className="mt-1 text-3xl font-bold tracking-tight">Assinatura</h1><p className="mt-2 text-muted-foreground">Escolha o plano que acompanha o crescimento da sua operação.</p><div className="mt-8 grid gap-5 lg:grid-cols-3">{plans.map((p,i)=><Card key={p.name} className={i===1?"border-primary shadow-lg shadow-primary/10":""}>{i===1&&<span className="rounded-full bg-primary/15 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wider text-primary">Mais escolhido</span>}<h2 className="mt-3 text-xl font-bold">{p.name}</h2><p className="mt-1 text-sm text-muted-foreground">{p.desc}</p><p className="mt-6 text-3xl font-bold">{p.price}<span className="text-sm font-normal text-muted-foreground">/mês</span></p><button className="mt-6 w-full rounded-lg bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground">Começar teste grátis</button><ul className="mt-6 space-y-3">{p.features.map(f=><li key={f} className="flex items-center gap-2 text-sm"><Check className="h-4 w-4 text-emerald-500" />{f}</li>)}</ul></Card>)}</div></div></AppShell> }
